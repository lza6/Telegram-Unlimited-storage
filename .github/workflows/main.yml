name: Web and Headless Publish

on:
  workflow_dispatch:

permissions:
  contents: read

# The product line is Web UI + Headless API. Automatic desktop/Tauri publishing
# is intentionally disabled; keep the legacy shell in source for compatibility.
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tauri publishing is disabled for the Web/Headless release line."
