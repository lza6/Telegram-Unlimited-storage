[package]
name = "app"
version = "4.0.0-beta"
description = "Turn your Telegram account into an unlimited, secure cloud storage drive. an Open-source desktop app built with Tauri, Rust, and React."
authors = ["Cameron Amer"]
edition = "2021"



[lib]

name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", default-features = false, features = [], optional = true }
tauri-plugin-opener = { version = "2", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
grammers-client = { git = "https://github.com/Lonami/grammers", rev = "d07f96f", features = ["proxy"] }
grammers-session = { git = "https://github.com/Lonami/grammers", rev = "d07f96f" }
grammers-mtsender = { git = "https://github.com/Lonami/grammers", rev = "d07f96f", features = ["proxy"] }
grammers-tl-types = { git = "https://github.com/Lonami/grammers", rev = "d07f96f" }
tokio = { version = "1", features = ["full"] }

tauri-plugin-store = { version = "2", optional = true }
tauri-plugin-window-state = { version = "2", optional = true }
tauri-plugin-shell = { version = "2", optional = true }
tauri-plugin-dialog = { version = "2.6.0", optional = true }
tauri-plugin-fs = { version = "2", optional = true }
chrono = "0.4"
base64 = "0.21"
log = "0.4"
env_logger = "0.11"
tauri-plugin-updater = { version = "2.9.0", optional = true }
actix-web = "4"
actix-cors = "0.7"
futures = "0.3"
async-stream = "0.3"
actix-rt = "2"
tauri-plugin-process = { version = "2.3.1", optional = true }
rand = "0.8"
sha2 = "0.10"
hmac = "0.12"
argon2 = "0.5"
zip = { version = "2", default-features = false, features = ["deflate"] }
walkdir = "2"
sqlite = "0.37.0"
actix-files = "0.6"
actix-multipart = "0.7"
futures-util = "0.3"
uuid = { version = "1", features = ["v4"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "multipart", "rustls-tls", "stream"] }
urlencoding = "2"
redis = { version = "0.27", default-features = false, features = ["script"] }
tokio-postgres = "0.7"
r2d2 = "0.8"
actix-ws = "0.3"
parking_lot = "0.12"
async-trait = "0.1"
tempfile = "3"

[[bin]]
name = "app"
path = "src/main.rs"
required-features = ["desktop"]

[[bin]]
name = "telegram-drive-server"
path = "src/bin/telegram-drive-server.rs"
required-features = ["headless-server"]

[features]
default = ["desktop"]
# Keep the desktop feature set equivalent to Tauri's previous defaults while
# allowing the headless server to compile without Linux GUI libraries.
desktop = [
  "dep:tauri",
  "tauri/wry",
  "tauri/compression",
  "tauri/common-controls-v6",
  "tauri/dynamic-acl",
  "tauri/x11",
  "dep:tauri-plugin-opener",
  "dep:tauri-plugin-store",
  "dep:tauri-plugin-window-state",
  "dep:tauri-plugin-shell",
  "dep:tauri-plugin-dialog",
  "dep:tauri-plugin-fs",
  "dep:tauri-plugin-updater",
  "dep:tauri-plugin-process",
]
headless-server = []
